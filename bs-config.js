// Used only for local development

module.exports = {
    files: './**/*', // Watch all files in the current directory and subdirectories
    server: {
        baseDir: './', // Serve files from the current directory
    },
    open: true, // Automatically open the browser
};
